import React from 'react';
import { Typography } from '@mui/material';

const MyAccount: React.FC = () => {
  return (
    <Typography variant="h4">My Account Page</Typography>
  );
};

export default MyAccount;
